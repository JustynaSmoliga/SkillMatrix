import { describe, it, expect } from 'vitest'

// import { mount } from '@vue/test-utils'
// import TheButtonSection from '../buttonsSection/TheButtonsSection.vue'

describe('TheButtonSection', () => {
  it('renders properly', () => {
    // const wrapper = mount(TheButtonSection, { props: { isDisabled: false } })
    // expect(wrapper.text()).toContain('Hello Vitest')
    expect(4).equal(4);
  })
})



// import { describe, it, expect } from 'vitest'

// import { mount } from '@vue/test-utils'
// import HelloWorld from '../HelloWorld.vue'

// describe('HelloWorld', () => {
//   it('renders properly', () => {
//     const wrapper = mount(HelloWorld, { props: { msg: 'Hello Vitest' } })
//     expect(wrapper.text()).toContain('Hello Vitest')
//   })
// })
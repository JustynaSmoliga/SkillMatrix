import { describe, it, expect } from 'vitest'
// import { mount } from '@vue/test-utils'
// import TheButtonSection from './TheButtonsSection.vue'

describe('TheButtonSection', () => {
  it('renders button section properly', () => {
    // const wrapper = mount(TheButtonSection, { props: { isDisabled: false } });
    // const editButton=wrapper.get('[class="btn--primary btn"]');
    // console.log(wrapper);
    // expect(wrapper.text()).toContain('Hello Vitest')
    // expect(editButton.).equal('Edit');
    expect(1).equal(1);
  })
})




// import { mount } from '@vue/test-utils'
// import HelloWorld from '../HelloWorld.vue'

// describe('HelloWorld', () => {
//   it('renders properly', () => {
//     const wrapper = mount(HelloWorld, { props: { msg: 'Hello Vitest' } })
//     expect(wrapper.text()).toContain('Hello Vitest')
//   })
// })